# PromoGuard v0.5.2 — Park reviewer Demo Mode

This release adds a one-click, real-data Streamlit presentation path on top of the verified 0.5.1
analytical foundation.

## Delivered

- explicit `streamlit run apps/dashboard/app.py -- --demo` launch;
- hidden local filesystem path and collapsed presentation sidebar;
- three-step Persian reviewer flow;
- deterministic representative-event selection under AuditPolicy 1.0.0;
- observed versus baseline visualization with baseline uncertainty interval;
- Persian recommendation, warning explanations, and claim-boundary card;
- generic failure state with no stack trace or personal path;
- downloadable original typed JSON result.

## Verification

- 73 tests, Ruff, and compileall pass;
- six pure presentation tests prove chart records copy typed domain values;
- the full 524,950-row public panel completed the one-click browser path;
- browser text inspection found no Windows user-home path and no `Traceback`;
- screenshots 03 and 04 in the Park package capture the event/result and chart/warnings views.

No causal, financial, customer-impact, production-readiness, or LLM-runtime claim was added.
