# پرامپت آماده برای Gemini Notebook — ارائه صوتی مسیر PromoGuard

این متن را در Gemini Notebook قرار بده. تمام فایل‌های پروژه و منابعی که در بخش منابع آمده‌اند را به Notebook اضافه کن. هدف تولید یک فایل صوتی آموزشی و تصمیم‌محور است، نه خلاصه‌سازی سطحی.

## نقش و هدف

تو یک دستیار ارشد Data Science، پژوهشگر محصول و مربی مسیر شغلی هستی. باید پروژه PromoGuard AI و مسیر توسعه آن را برای رضا، دانشجوی ترم چهارم آمار بدون سابقه کار صنعتی، به زبان فارسی و بسیار ساده اما دقیق ارائه کنی.

شنونده باید بعد از گوش دادن بداند:

1. PromoGuard دقیقاً چه مسئله واقعی را حل می‌کند.
2. هر بخش نرم‌افزار و آماری چه کاری انجام می‌دهد.
3. با داده عمومی واقعی چه چیزی قابل اثبات است و چه چیزی قابل اثبات نیست.
4. در هر فاز چه چیزی ساخته می‌شود، چرا ساخته می‌شود، چگونه ارزیابی می‌شود و خروجی آن چیست.
5. این پروژه چگونه هم‌زمان برای پارک علم و فناوری دانشگاه تهران، مسئله شماره ۶۵ کارخانه هوش مصنوعی، و موقعیت‌های شغلی Data Scientist و Applied AI رزومه می‌سازد.
6. رضا در هر مرحله چه مباحثی را باید تمرین کند و چه چیزی را بتواند در مصاحبه توضیح دهد.

## قانون صحت و منبع

قبل از ارائه، هر ادعای مربوط به تاریخ ثبت‌نام، شرایط پذیرش، مسئله شماره ۶۵ کارخانه هوش مصنوعی، پارک علم و فناوری، AIIF، مجوز داده یا مدل را با منبع رسمی بررسی کن.

در فایل `aiif-106-triage-fa.md`، مسئله شماره ۶۵ با عنوان «اثر تخفیف/پروموشن/هم‌خوری» ثبت شده و در ارزیابی داخلی پروژه، دقیق‌ترین اتصال آمار، causal inference، forecasting، سود، LLM و نیاز صنعتی معرفی شده است. این مسئله را همان #65 کارخانه هوش مصنوعی بدان؛ آن را با «ره‌نشان ۱۴۰۵» یا یک رویداد جداگانه اشتباه نگیر. اگر صفحه رسمی کارخانه هوش مصنوعی یا شرایط جاری مسئله در منابع Notebook موجود نیست، فقط جزئیات رسمی احراز و ددلاین را «نیازمند تأیید» اعلام کن و از حدس‌زدن خودداری کن.

هیچ‌وقت تاریخ، جایزه، معیار احراز، شریک تجاری، نتیجه مدل، درصد افزایش فروش یا مصاحبه مشتری را حدس نزن.

در تمام ارائه این تفاوت را تکرار کن:

- داده عمومی و ناشناس برای بازتولید روش و ارزیابی مهندسی مناسب است.
- داده مشاهده‌ای برای اثبات اثر علی کافی نیست.
- داده dunnhumby منبع اصلی promotion analysis است، اما ناشر آن را representation/inspired real-world معرفی می‌کند، نه داده خام شرکت.
- M5 برای forecasting و price features مناسب است، اما هر تغییر قیمت promotion نیست.
- Criteo Uplift برای treatment-effect/uplift benchmark است، نه فروشگاه و SKU.
- FreshRetailNet-50K برای stockout و censored demand است، نه الزاماً promotion.
- اثر تجاری واقعی در ایران فقط با design partner و آزمایش تأییدشده قابل ادعاست.

## منابعی که باید مبنا قرار بگیرند

منابع محلی پروژه:

- `README.md`
- `ROADMAP.md`
- `DATA_SOURCES.md`
- `IMPLEMENTATION_AGENT_PROMPT.md`
- `docs/problem-brief.md`
- `docs/evaluation-protocol.md`
- `docs/limitations.md`
- `docs/model-card.md`
- `docs/decisions/0001-initial-scope.md`
- `promoguard-park-draft-fa.md`
- `aiif-106-triage-fa.md`
- `triple-path-strategy-fa.md`

منابع داده:

- صفحه رسمی dunnhumby: https://www.dunnhumby.com/source-files/
- صفحه M5: https://www.kaggle.com/competitions/m5-forecasting-accuracy/data
- صفحه رسمی Criteo: https://ailab.criteo.com/criteo-uplift-prediction-dataset/
- دانلود Criteo: http://go.criteo.net/criteo-research-uplift-v2.1.csv.gz
- FreshRetailNet-50K: https://huggingface.co/datasets/Dingdong-Inc/FreshRetailNet-50K
- مقاله FreshRetailNet: https://arxiv.org/abs/2505.16319

## سبک صوتی

- زبان: فارسی روان، محاوره‌ای محترمانه، بدون ترجمه ماشینی خشک.
- گوینده باید مثل یک منتور آرام، دقیق و واقع‌گرا صحبت کند.
- اصطلاحات فنی را انگلیسی داخل پرانتز بیاور و بلافاصله با مثال توضیح بده.
- هر مفهوم را اول با مثال فروشگاه توضیح بده، بعد تعریف فنی بده.
- از ادعای «پروژه بی‌نقص»، «تضمین استخدام» و «تضمین پذیرش» استفاده نکن.
- به‌جای وعده، شواهد قابل ارائه را توضیح بده.
- بین فصل‌ها مکث کوتاه و جمله انتقالی بگذار.
- هر فصل را با این سه جمله تمام کن: «هدف این فصل»، «خروجی قابل مشاهده»، «چیزی که رضا باید تمرین کند».
- خروجی را در یک فایل صوتی پیوسته با فصل‌های واضح تولید کن؛ در صورت محدودیت، آن را به چند اپیزود شماره‌دار تقسیم کن.

## ساختار اجباری ارائه صوتی

### فصل ۱ — تصویر کلی و مسئله

توضیح بده چرا دیدن رشد فروش بعد از تخفیف به معنای موفقیت تخفیف نیست. مثال بزن که رشد فروش ممکن است از فصل‌گرایی، خرید جلوتر مشتری، انتقال فروش از SKU دیگر، تغییر توزیع، کمبود موجودی یا انتخاب زمان تخفیف ایجاد شده باشد.

سپس PromoGuard را در یک جمله تعریف کن:

«یک سیستم حسابرسی تصمیم‌یار برای بررسی اثر مشاهده‌شده پروموشن است که قبل از توصیه، کیفیت داده، baseline، عدم‌قطعیت و محدودیت علی را بررسی می‌کند.»

توضیح بده محصول در نسخه اول قرار نیست خودش تخفیف را اجرا کند یا ادعا کند فروش را علت‌یابی قطعی کرده است.

### فصل ۲ — چرا این پروژه برای یک دانشجوی آمار ارزشمند است

ارتباط پروژه را با این مباحث توضیح بده:

- احتمال و توزیع‌ها
- رگرسیون
- سری زمانی
- طراحی آزمایش
- causal inference
- فاصله اطمینان و uncertainty
- بهینه‌سازی
- پایگاه داده و SQL
- مهندسی نرم‌افزار و تست
- LLM و structured output

توضیح بده که مزیت رضا فقط ساختن مدل نیست؛ مزیت اصلی، نشان دادن زنجیره کامل از سؤال کسب‌وکار تا تصمیم قابل حسابرسی است.

### فصل ۳ — داده واقعی و انتخاب منابع

اول dunnhumby Breakfast at the Frat را باز کن و توضیح بده چه ستون‌ها و چه grainی دارد. تأکید کن که منبع اصلی هفتگی است و نباید آن را به‌صورت جعلی روزانه کنیم.

توضیح بده:

- raw data کجا ذخیره می‌شود.
- processed data چیست.
- چرا hash فایل، تاریخ دانلود، license/terms و schema ثبت می‌شود.
- چرا داده دانلودی نباید وارد Git شود.
- چرا نبودن cost یا inventory باید به‌صورت محدودیت گزارش شود.

سپس کاربرد جداگانه M5، Criteo و FreshRetailNet را توضیح بده و بگو چرا این‌ها را در یک پنل جعلی ادغام نمی‌کنیم.

### فصل ۴ — معماری نرم‌افزار

این جریان را مرحله‌به‌مرحله شرح بده:

```text
real CSV files
  → contracts and validation
  → canonical weekly panel
  → time-aware baseline forecast
  → promotion audit
  → cannibalization and forward-buy diagnostics
  → uncertainty and abstention
  → constrained scenarios
  → typed insight JSON
  → FastAPI and Streamlit
```

برای هر لایه بگو ورودی، خروجی، مسئولیت و تست آن چیست. توضیح بده چرا محاسبات نباید داخل dashboard یا API پراکنده شوند و چرا `src/promoguard` هسته پروژه است.

### فصل ۵ — فاز ۰: اسکلت و سیاست علمی

توضیح بده در این فاز چه چیزهایی ساخته شده: package، API health، dashboard placeholder، تست‌ها، CI، problem brief، evaluation protocol، model card و limitations.

مزیت این فاز: جلوگیری از ادعاهای بی‌پشتوانه و آماده‌سازی ساختار قابل review.

### فصل ۶ — فاز ۱: دریافت و اعتبارسنجی داده واقعی

جزئیات اجرایی را شرح بده:

1. رضا فایل Breakfast at the Frat را از صفحه رسمی دانلود می‌کند.
2. فایل در `data/raw/breakfast-at-the-frat/` قرار می‌گیرد.
3. عامل ساختار فایل‌ها را inspect می‌کند.
4. schema، نوع ستون‌ها، missing values، duplicateها و date range بررسی می‌شوند.
5. داده به `data/processed/` تبدیل می‌شود.
6. quality report تولید می‌شود.
7. تست‌ها خطاهای واقعی مثل مقدار منفی، قیمت نامعتبر، تاریخ نادرست و duplicate grain را بررسی می‌کنند.

توضیح بده چرا این فاز برای موقعیت Junior Applied AI و Data Scientist مهم‌تر از اضافه کردن یک LLM است.

### فصل ۷ — فاز ۲: baseline و forecasting

با مثال توضیح بده baseline یعنی «اگر هیچ پروموشنی نبود، انتظار داشتیم فروش چقدر باشد؟» سپس توضیح بده:

- seasonal naive چیست.
- rolling-origin evaluation چیست.
- چرا random split در سری زمانی اشتباه است.
- WAPE، MASE، bias و interval coverage چه می‌گویند.
- leakage چگونه رخ می‌دهد.

توضیح بده خروجی این فاز پیش‌بینی است، نه هنوز ادعای causal effect.

### فصل ۸ — فاز ۳: promotion audit

توضیح بده چگونه فروش مشاهده‌شده با baseline مقایسه می‌شود:

```text
observed sales − expected baseline sales = observed incremental estimate
```

تأکید کن این عبارت در داده مشاهده‌ای «برآورد حسابرسی» است، نه «اثر قطعی ناشی از پروموشن».

توضیح بده سیستم چگونه pre-period، during-period و post-period را بررسی می‌کند و چگونه stockout، missing cost، short history و shift را به هشدار تبدیل می‌کند.

### فصل ۹ — فاز ۴: API و داشبورد

سناریوی دمو را روایت کن:

1. کاربر فایل را وارد می‌کند.
2. quality report را می‌بیند.
3. یک محصول یا پروموشن را انتخاب می‌کند.
4. baseline و فروش مشاهده‌شده را می‌بیند.
5. uncertainty و warningها را می‌خواند.
6. سیستم یکی از `approve`، `reject` یا `experiment` را با دلیل نشان می‌دهد.

توضیح بده چرا recommendation ممکن است `experiment` باشد و چرا abstention نشانه ضعف نیست، بلکه نشانه بلوغ علمی سیستم است.

### فصل ۱۰ — فاز ۵: بسته اپلای پارک

توضیح بده بسته پارک باید شامل این بخش‌ها باشد:

- مسئله واقعی و مشتری هدف
- راه‌حل و تفاوت با dashboard معمولی
- وضعیت MVP
- معماری
- روش ارزیابی
- مدل درآمد یا مسیر پایلوت
- ریسک‌ها و محدودیت‌ها
- برنامه ۹۰ روزه
- درخواست مشخص از پارک
- ویدئوی دمو و screenshots

روشن کن که بدون مصاحبه واقعی نباید traction ساختگی نوشته شود. به‌جای آن، hypothesis و برنامه مصاحبه ثبت می‌شود.

### فصل ۱۱ — فازهای حرفه‌ای بعد از ارسال پارک

فاز ۶: استفاده از Criteo برای benchmark واقعی uplift و جدا نگه داشتن آن از retail.

فاز ۷: تشخیص cannibalization، substitution، forward-buy، uncertainty و abstention.

فاز ۸: مقایسه سناریوهای تخفیف با محدودیت budget، margin و inventory؛ بدون اجرای خودکار کمپین.

فاز ۹: DuckDB/PostgreSQL، pipeline idempotent، logging، monitoring و لایه اختیاری LLM. LLM فقط باید JSON محاسبه‌شده را توضیح دهد و حق تغییر عدد را نداشته باشد.

فاز ۱۰: README دوزبانه، case study، model card، limitations، ویدئو، رزومه و mapping به آگهی‌های شغلی.

### فصل ۱۲ — زمان‌بندی

با تاریخ فعلی Notebook و منبع رسمی، تاریخ‌ها را بررسی کن. در نسخه فعلی پروژه، مهلت هدف پارک به‌صورت برنامه‌ریزی‌شده ۱۳ شهریور ۱۴۰۵، برابر با ۴ سپتامبر ۲۰۲۶، ثبت شده است؛ اگر فراخوان رسمی تاریخ دیگری دارد، تاریخ رسمی را جایگزین کن.

برنامه فشرده را توضیح بده:

- ۸ شهریور: داده و validation
- ۹ شهریور: baseline و forecasting
- ۱۰ شهریور: promotion audit
- ۱۱ شهریور: API و dashboard
- ۱۲ شهریور: بسته ارائه و دمو
- ۱۳ شهریور: کنترل نهایی و ارسال

صریح بگو این نسخه، MVP برای ارسال است؛ نسخه production کامل در پنج روز واقع‌بینانه نیست.

### فصل ۱۳ — رزومه‌ای که ساخته می‌شود

توضیح بده پروژه چگونه این شواهد را ایجاد می‌کند:

- Python: package، tests، data processing و API
- SQL: canonical panel، aggregation و warehouse queries
- Statistics: regression، time series، confidence intervals
- Causal: identification assumptions و diagnostic checks
- ML: forecasting، ranking یا uplift در فازهای بعد
- Data Engineering: ingestion، contracts، quality، idempotency
- MLOps: monitoring، drift، reproducibility
- GenAI: typed JSON، guardrails، evaluation و grounded explanation
- Product: dashboard، decision states، pilot hypothesis

توضیح بده در رزومه فقط مواردی نوشته شود که در repository و demo واقعاً قابل مشاهده‌اند.

### فصل ۱۴ — برنامه تمرین رضا

برای هر هفته تمرین‌های عملی بده:

- هفته اول: pandas، SQL group by/window functions، schema و missingness
- هفته دوم: seasonal baseline، rolling split، WAPE/MASE
- هفته سوم: regression، confounding، pre-trend و placebo
- هفته چهارم: API، Pydantic، pytest، Git و Docker
- هفته پنجم: uplift، CATE، overlap و calibration
- هفته ششم: uncertainty، drift، abstention و model card
- هفته هفتم: LLM structured output، prompt injection، numerical grounding و evaluation

برای هر موضوع یک سؤال مصاحبه‌ای و یک جواب ساده اما فنی پیشنهاد کن.

### فصل ۱۵ — مزیت رقابتی و چالش‌ها

مزیت‌ها:

- تمرکز روی تصمیم اقتصادی واقعی
- تفکیک forecasting از causal inference
- داده واقعی عمومی با provenance
- abstention و محدودیت‌های شفاف
- مسیر کامل از داده تا محصول
- مناسب برای رزومه آماری و Applied AI

چالش‌ها:

- داده اصلی ممکن است هزینه و موجودی نداشته باشد.
- داده عمومی الزاماً بازار ایران نیست.
- رابطه مشاهده‌ای پروموشن و فروش با causal effect تفاوت دارد.
- داده هفتگی برای daily demand کافی نیست.
- اتصال LLM بدون guardrail خطر hallucination دارد.
- کاربر تک‌نفره باید scope را کنترل کند.

برای هر چالش، راه‌حل واقع‌بینانه و جمله قابل استفاده در جلسه داوری ارائه کن.

### فصل ۱۶ — جمع‌بندی یک‌تیر و سه‌نشان

در پایان این زنجیره را شفاف جمع‌بندی کن:

```text
یک مسئله واقعی retail
  → یک MVP قابل اجرا
  → یک گزارش پژوهشی و آماری
  → یک بسته اپلای برای پارک/چالش
  → یک portfolio case study
  → چندین شاهد قابل سنجش برای رزومه شغلی
```

اما تأکید کن که سه هدف با «یک پروژه» پوشش داده می‌شوند، نه با یک ادعای مشترک و اغراق‌شده. هر نهاد باید evidence مناسب خودش را ببیند.

## تنظیمات پیشنهادی Gemini Notebook

- همه منابع محلی و لینک‌های رسمی بالا را اضافه کن.
- زبان خروجی را فارسی انتخاب کن.
- حالت صوتی را روی توضیح عمیق/تحلیلی بگذار.
- از خلاصه‌سازی کوتاه استفاده نکن.
- اگر تنظیمی برای طول وجود دارد، طول زیاد یا حداکثر مجاز را انتخاب کن.
- از Notebook بخواه قبل از تولید صوت، outline فصل‌ها و فهرست ادعاهای نیازمند منبع را نشان دهد.
- سپس از آن بخواه نسخه صوتی را تولید کند.
- اگر امکان انتخاب گوینده وجود دارد، گوینده‌ای با سرعت متوسط، لحن آموزشی و تلفظ روشن فارسی انتخاب کن.
- از موسیقی پس‌زمینه استفاده نکن یا آن را بسیار کم بگذار.
- برای هر فصل عنوان صوتی و مکث کوتاه بگذار.
- اگر متن بیش از ظرفیت صوتی بود، آن را به چهار اپیزود تقسیم کن: مسئله و داده، معماری و آمار، محصول و اپلای، رزومه و تمرین.

## قالب خروجی مورد انتظار از Notebook

ابتدا یک خلاصه یک‌دقیقه‌ای بده، سپس ارائه کامل را تولید کن. در انتها این موارد را به‌صورت فهرست صوتی جمع‌بندی کن:

1. مهم‌ترین کار امروز رضا
2. مهم‌ترین خروجی قابل تحویل این هفته
3. مهم‌ترین ریسک پروژه
4. مهم‌ترین مبحثی که باید تمرین شود
5. وضعیت تاریخ اپلای و اینکه آیا از منبع رسمی تأیید شده یا نه
6. فایل‌ها و artifactهایی که باید در GitHub دیده شوند
