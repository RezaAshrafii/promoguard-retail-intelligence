services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: promoguard
      POSTGRES_USER: promoguard
      POSTGRES_PASSWORD: promoguard
    ports:
      - "5432:5432"
    volumes:
      - promoguard_pgdata:/var/lib/postgresql/data

volumes:
  promoguard_pgdata:

